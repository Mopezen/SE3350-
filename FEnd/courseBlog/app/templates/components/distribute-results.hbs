<div class="large-12  large-centered columns">
    <div class="panel">
        <table>
            <thead>
                <tr>
                    <th width="200">Student Name</th>
                    <th width="150">Student Number</th>
                    <th width="300">Results</th>
                </tr>
            </thead>
            <tbody>
                {{#each distributionResults as |oneStudent|}}
                    <tr>
                        <td>{{oneStudent.firstName}} {{oneStudent.lastName}}</td>
                        <td>{{oneStudent.number}}</td>
                        <td>{{oneStudent.cumAverage}}<!-- {{calculateAverage oneStudent.id store}} --></td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>